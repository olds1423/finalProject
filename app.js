//starting the form JS

//Setup page; capturing user inputs i.e. rent + $, water + $

//we need to display people in one form
// we need to display the bills in another formr

var form = document.getElementById('form');

function handleFormSubmit() {
  if (true) {

  }
}
//simplified graph
